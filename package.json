{
  "name": "remote-executor-mcp",
  "version": "2.0.0",
  "description": "MCP Server for remote code execution via SSH",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "remote-executor-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc -w"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
